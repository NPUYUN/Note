# IDEA集成Git
## 配置Git忽略文件
### 为什么要忽略他们
与项目的实际功能无关，不参与服务器的部署运行，且忽略掉能够屏蔽IDE工具之间的差距

### 怎么忽略
1. 创建忽略规则文件 **xxx.ignore**，将忽略的文件后缀写入文件即可，这里略过
2. 在.gitconfig文件中引用忽略配置文件
```bash
[core]
    excludesfile = git.ignore位置
```

### 定位Git程序
在IDEA设置中的版本控制中定位Git程序

## 本地库操作
### 初始化本地库
在项目版本控制(VSC)选项中选择