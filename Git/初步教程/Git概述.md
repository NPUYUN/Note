# Git概述
Git是一个免费的、开源式的**分布式**版本控制系统，可以快速高效的处理各种项目
> [官网](http://www.git-scm.com)

---

## 版本控制
版本控制是一种记录文件内容变化，以便将来查阅特定的版本修订情况的系统。其主要用于记录**文件修改**的**历史记录**，从而让用户能够查看历史版本，方便**版本切换**

### 为什么需要版本控制
从个人开发过程过渡到团队协作

### 版本控制工具
#### 集中式版本控制工具(SVN、CVS等)
单一的集中管理的服务器，保存所有文件的修订版本，协作人员通过客户端连接到服务器取出最新的文件或者提交更新。

- 好处：每个人都可以一定程度上看到其他人在做什么，管理员也可以掌握每个开发者的权限。

- 坏处：服务器单点故障则无法导致协同工作

#### 分布式版本控制工具(Git 等)
客户端提取时把代码仓库完整的镜像下来（本地库），即对整个文件仓库的完整备份

- 版本控制本地进行，保证服务器故障也能开发
- 每个客户端保存完整项目，包含历史记录，更加安全

## Git工作机制
``` mermaid
graph TD
    A[工作区：写代码] -->|git add| B[暂存区：临时存储]
    B -->|git commit| C[本地库：历史版本]
    C --> D[远程库]
```

## Git和代码托管中心（远程库）
代码托管中心是基于网络服务器的远程代码仓库
- 局域网：GitLab
- 互联网：GitHub、Gitee